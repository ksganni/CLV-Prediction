services:
  clv-dashboard:
    build: .
    ports:
      - "8501:8501"
    volumes:
      # Live-edit code without rebuilding (optional)
      - ./src:/app/src
      - ./data:/app/data
      - ./outputs:/app/outputs
    restart: unless-stopped
